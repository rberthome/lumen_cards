# Un seul service : app Next + SQLite sur un volume. Réf. PROJECT.md §8.
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: "file:/data/prod.db"
    volumes:
      - lumen_data:/data
    restart: unless-stopped

volumes:
  lumen_data:
