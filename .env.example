# SQLite — fichier sur volume Docker en prod (réf. PROJECT.md §8)
DATABASE_URL="file:./dev.db"

# Secret de signature des sessions (≥ 16 caractères) — générer une valeur unique en prod
SESSION_SECRET="dev-secret-change-me-32-chars-min!"
