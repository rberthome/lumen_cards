# LumenCards — Variables d'environnement (dev local)
# Copier en .env et ajuster si besoin

# Ports exposés
API_PORT=8001
WEB_PORT=3000
DB_PORT=5432
REDIS_PORT=6379
MAILPIT_PORT=8025

# Base de données
DB_DATABASE=lumen_cards
DB_USERNAME=lumen
DB_PASSWORD=secret
