{
  "name": "lumen_cards",
  "private": true,
  "workspaces": [
    "apps/mobile",
    "packages/*"
  ],
  "scripts": {
    "mobile": "npm run start --workspace=apps/mobile",
    "types:generate": "cd apps/api && docker compose exec app php artisan typescript:transform && cp resources/ts/generated.d.ts ../../packages/types/generated.d.ts",
    "lint": "npm run lint --workspace=apps/mobile",
    "typecheck": "npm run typecheck --workspace=apps/mobile"
  },
  "devDependencies": {
    "prettier": "^3.8.4"
  }
}
